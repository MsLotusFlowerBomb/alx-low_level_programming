#include "main.h"
#include <stdio.h>

/**
 * print_rev - prints string in reverse
 * @s: String to print
 * Return: void
 */

void print_rev(char *s)
{
	int c = 0;

	while (s[c] != '\0')
	{
		c++
	}

	for (c--; a >= 0; a--)
	_putchar(s[a]);
	_putchar('\n');
}
